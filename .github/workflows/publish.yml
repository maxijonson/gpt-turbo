name: Publish

on:
  workflow_dispatch:
    inputs:
      type:
        type: choice
        options:
          - patch
          - minor
          - major
        description: "Release type"
        required: true

jobs:
  release-manual:
    name: Manual release
    if: github.ref_name == 'master' && github.event_name == 'workflow_dispatch' && inputs.type != ''
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm install

      - name: Set Git identity
        run: |
          git config user.name "GitHub Actions"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git

      - name: Release
        run: npm run release:${{ inputs.type }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
