{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "lerna watch --scope \"@maxijonson/gpt-turbo\" --include-dependencies -- lerna run build --scope=\"@maxijonson/gpt-turbo\""
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
