{
  "name": "root",
  "scripts": {
    "build": "lerna run build",
    "release": "npm run build && lerna publish",
    "dev": "lerna run build --scope \"@maxijonson/gpt-turbo\" && lerna watch --scope \"@maxijonson/gpt-turbo\" --include-dependencies -- lerna run build --scope=\"@maxijonson/gpt-turbo\""
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  }
}
