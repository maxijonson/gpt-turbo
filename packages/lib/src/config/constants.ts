export const DEFAULT_MODEL = "gpt-3.5-turbo" as const;

export const DEFAULT_CONTEXT =
    "You are a large language model trained by OpenAI. Answer as concisely as possible." as const;

export const DEFAULT_DRY = false as const;

export const DEFAULT_DISABLEMODERATION = false as const;

export const DEFAULT_STREAM = false as const;

export const COST_PER_TOKEN = 0.000002 as const;
