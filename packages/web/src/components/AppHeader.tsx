import { Header } from "@mantine/core";

export const HEADER_HEIGHT = 60;

export default () => {
    return (
        <Header height={HEADER_HEIGHT} p="xs">
            Header
        </Header>
    );
};
